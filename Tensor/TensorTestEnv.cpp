# include "TensorTest.h"

int main()
{
	miniflow::Shape<3> shape = { miniflow::Index(2),
								 miniflow::Index(3),
								 miniflow::Index(4) };


	miniflow::Tensor<1> a(2);
	int i = 0;

	return 0;
}